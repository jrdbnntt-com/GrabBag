<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.jrdbnntt.cop4656.grabbag">

    <uses-permission android:name="android.permission.INTERNET" />

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/AppTheme">

        <!-- android:theme="@style/Theme.AppCompat.Light.NoActionBar" -->
        <activity
            android:name=".app.SplashScreenActivity"
            android:theme="@style/AppTheme1">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity android:name=".app.CreateAccountActivity" />
        <activity android:name=".app.LoginActivity" />
        <activity android:name=".app.GroupChoiceActivity" />
        <activity android:name=".app.JoinGroupActivity" />
        <activity android:name=".app.CreateGroupActivity" />
        <activity android:name=".app.GroupScreenActivity" />
        <activity android:name=".app.AttackScreenActivity" />
        <activity android:name=".app.MainScreen" />
        <!-- Firebase services -->
        <service
            android:name=".GrabBagFirebaseMsgServ"
            android:enabled="true"
            android:exported="true">
            <intent-filter>
                <action android:name="com.google.firebase.MESSAGING_EVENT" />
            </intent-filter>
        </service>
        <service
            android:name=".GrabBagInstIDServ"
            android:enabled="true"
            android:exported="true">
            <intent-filter>
                <action android:name="com.google.firebase.INSTANCE_ID_EVENT" />
            </intent-filter>
        </service>
    </application>

</manifest>